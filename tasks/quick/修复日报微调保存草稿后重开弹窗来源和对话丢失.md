---
forge_loop: true
artifact: quick-task
slug: 修复日报微调保存草稿后重开弹窗来源和对话丢失
status: done
mode: fix
blocking: false
---

# Quick Task: 修复日报微调保存草稿后重开弹窗来源和对话丢失

## Intent

| Field | Value |
| --- | --- |
| Status | done |
| Mode | fix |
| Spike Type | N/A |
| Request | 保存微调草稿并加入新来源后，再打开日报微调，新增引用和之前对话丢失。 |
| Owner | Codex |
| Created | 2026-04-29 |
| Risk | low |
| Escalation | none |
| Upgrade Summary | N/A |

## Scope

- 修复日报微调 session restore 的状态过滤。
- 补充保存后恢复新增来源和对话的回归测试。

## Out of Scope

- 不改数据库 schema，不改保存接口契约，不改 AI 调用逻辑。

## Acceptance

- 保存候选稿后再次打开日报微调，仍能恢复最近保存的微调 session。
- 右侧来源上下文仍包含保存前加入的新来源。
- 之前的对话消息仍可见。

## Domain Language

| Term | Meaning / Source |
| --- | --- |
| 日报微调 | 当前功能名称 |
| 新增召回 | 微调会话中加入未入选候选源 |

## Feedback Loop

| Field | Value |
| --- | --- |
| Loop Type | service test |
| Command | `npm test -- tests/integration/daily-report-service.test.ts -t "recalls unselected sources"` |
| Failure Signal | 保存后 `getLatestDailyReportRefinementSession()` 返回 null 或 sourceRegistry 缺少新来源。 |
| Determinism | deterministic |
| Re-run Plan | 保存含召回来源的候选稿后恢复 latest session。 |

## Debugging Evidence

| Field | Value |
| --- | --- |
| Repro Steps | 打开 `http://localhost:3001/daily/2026-04-29`，日报微调中新增召回来源，生成并保存草稿，关闭后再打开微调弹窗。 |
| Observed Failure | 新增引用和之前对话消失。 |
| Expected Behavior | 保存后的微调 session 仍可恢复，包含对话消息和扩展后的 sourceRegistry。 |
| Root Cause | `saveDailyReportRefinementCandidate()` 将 session 标为 `saved`，但 `getLatestDailyReportRefinementSession()` 只查询 `status: "active"`，保存后的 session 被恢复接口排除。 |
| Fix Hypothesis | 恢复接口同时接受 `active` 和 `saved`，仅排除 `discarded`；新对话仍通过 discard 清理上下文。 |
| Regression Validation | 保存后恢复 session 的服务层测试。 |
| Failed Hypotheses | 0 |
| Handoff | N/A |

## Spike Findings

| Area | Finding |
| --- | --- |
| Module Map | N/A |
| Architecture Candidates | N/A |
| Issue Triage | N/A |
| Out Of Scope Match | N/A |

## Files Likely Touched

- `src/lib/daily-report/service.ts`
- `tests/integration/daily-report-service.test.ts`

## Execution

- Change latest session restore query to include `active` and `saved`.
- Extend existing source recall save test to verify restore after save.

### Changed Files

| File | Change |
| --- | --- |
| `src/lib/daily-report/service.ts` | `getLatestDailyReportRefinementSession()` 恢复查询同时包含 `active` 和 `saved` session，仍排除 `discarded`。 |
| `tests/integration/daily-report-service.test.ts` | 扩展新增召回保存链路测试，断言保存后恢复 session 仍包含新增来源和历史对话。 |

### Commands Run

| Command | Result | Notes |
| --- | --- | --- |
| `npm test -- tests/integration/daily-report-service.test.ts -t "recalls unselected sources"` | pass | 针对保存后恢复新增来源的聚焦回归，1 passed。 |
| `npm test -- tests/integration/daily-report-service.test.ts` | pass | 日报服务集成测试 13 passed。 |
| `npx tsc --noEmit` | pass | 类型检查通过。 |
| `npm run lint` | pass with warning | 仅保留既有 `src/components/admin/admin-page-client.tsx:133` `_props` 未使用 warning。 |
| `npm run build` | pass | Next production build 通过。 |
| `docker compose up -d --build` | pass | app/worker 镜像重建并启动。 |
| `sleep 3 && docker compose ps && curl -I http://localhost:3001/` | pass | app/worker Up，首页返回 `HTTP/1.1 200 OK`。 |
| `npx @shawnxie666/forge-loop validate --slug 修复日报微调保存草稿后重开弹窗来源和对话丢失` | pass | Quick Task 工作流产物校验通过。 |

## Result

done

## Follow-ups

- N/A

## Open Questions

| Question | Owner | Blocking | Resolution |
| --- | --- | --- | --- |
| N/A | N/A | no | N/A |

## Assumptions

- `saved` 表示该 session 已经成功保存过候选稿，但仍应作为最近上下文恢复；只有 `discarded` 才表示用户明确开启了新对话并丢弃旧上下文。

## Risks

- 如果长期保留 saved session，用户可能看到上次保存的候选稿预览；这与恢复上下文目标一致，后续可以单独优化展示文案。

## Validation

- Fresh command results are recorded in Commands Run.
