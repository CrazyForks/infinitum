---
forge_loop: true
artifact: quick-task
slug: 调整日报微调右侧栏-来源卡片和召回输入框背景色
status: done
mode: quick
blocking: false
---

# Quick Task: 调整日报微调右侧栏-来源卡片和召回输入框背景色

## Intent

| Field | Value |
| --- | --- |
| Status | done |
| Mode | quick |
| Spike Type | N/A |
| Request | 调整日报微调右侧栏、来源卡片和召回输入框背景色 |
| Owner | Codex |
| Created | 2026-04-29 |
| Risk | low |
| Escalation | none |
| Upgrade Summary | N/A |

## Scope

- 调整日报微调弹窗右侧栏背景、来源上下文条目卡片背景和新增召回输入框背景。

## Out of Scope

- 不调整对话逻辑、召回逻辑、弹窗布局结构或接口行为。

## Acceptance

- 右侧栏背景更接近主面板白底，来源条目和召回输入框使用浅灰承载层，层级与截图期望一致。

| Field | Value |
| --- | --- |
| Loop Type | static check |
| Command | `npx tsc --noEmit && npm run lint` |
| Failure Signal | 类型错误、lint error 或 Tailwind class 写法不合法。 |
| Determinism | deterministic |
| Re-run Plan | 样式调整后重新运行静态检查。 |

| Field | Value |
| --- | --- |
| Repro Steps | 打开日报微调弹窗，查看右侧新增召回和当前来源上下文区域。 |
| Observed Failure | 右侧栏为灰底，输入框和来源卡片为白底，视觉层级与期望相反。 |
| Expected Behavior | 右侧栏为白底，输入框和来源卡片使用浅灰底。 |
| Root Cause | 现有样式使用 `bg-[var(--surface-muted)]` 作为右侧栏背景，输入框和来源条目使用 `bg-[var(--surface)]`。 |
| Fix Hypothesis | 交换三处背景变量即可满足视觉层级调整，评分 badge 改回白底以保持可读性。 |
| Regression Validation | 静态检查通过。 |
| Failed Hypotheses | 0 |
| Handoff | N/A |

| Area | Finding |
| --- | --- |
| Module Map | N/A |
| Architecture Candidates | N/A |
| Issue Triage | N/A |
| Out Of Scope Match | N/A |

## Files Likely Touched

- `src/components/daily/daily-report-detail.tsx`

## Execution

- Locate right sidebar, recall input, and current source context item styles.
- Swap background variables to make sidebar white and inner controls/cards muted.
- Run static validation.

### Changed Files

| File | Change |
| --- | --- |
| `src/components/daily/daily-report-detail.tsx` | 右侧栏改白底；新增召回输入框、来源上下文条目改浅灰底；评分 badge 改白底。 |

### Commands Run

| Command | Result | Notes |
| --- | --- | --- |
| `npx tsc --noEmit` | pass | 类型检查通过。 |
| `npm run lint` | pass with warning | 仅保留既有 `src/components/admin/admin-page-client.tsx:133` `_props` 未使用 warning。 |
| `npx @shawnxie666/forge-loop validate --slug 调整日报微调右侧栏-来源卡片和召回输入框背景色` | pass | Quick Task 工作流产物校验通过。 |

## Result

done

## Follow-ups

- N/A

## Open Questions

| Question | Owner | Blocking | Resolution |
| --- | --- | --- | --- |
| N/A | N/A | no | N/A |

## Assumptions

- “换一下”按截图语义理解为交换右侧栏与其内部输入框、卡片的明暗层级。

## Risks

- 未运行浏览器截图验证；本次是局部样式变量替换，风险较低。

## Validation

- `npx tsc --noEmit` passed.
- `npm run lint` passed with one existing warning.
- Completion claim is based on the fresh command results in Commands Run.
